spring:
    # configuration de la datasource
  datasource:
    url: jdbc:postgresql://localhost:5433/EmoBib
    username: postgres
    password: pass
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        format_sql: true